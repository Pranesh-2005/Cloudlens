SECRET_KEY=change-me-to-a-long-random-string
ENCRYPTION_KEY=generate-with-python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Postgres (Neon/Supabase). Always create/use a database named "cloudlens".
# Neon example:     postgresql://user:pass@ep-xxxx.region.aws.neon.tech/cloudlens?sslmode=require
# Supabase example:  postgresql://postgres:pass@db.xxxx.supabase.co:5432/cloudlens
# Leave empty to fall back to local sqlite (dev-only, not for deploys).
DATABASE_URL=postgresql://neondb_owner:@ep-cold-resonance-aw53ozas-pooler.c-12.us-east-1.aws.neon.tech/cloudlens?sslmode=require&channel_binding=require
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
FRONTEND_ORIGIN=http://localhost:3000
DEMO_SEED=42
